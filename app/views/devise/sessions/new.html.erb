<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 offset-sm-1 col-md-6 offset-md-3 ">
      <div class="form-login">
        <h2>Bienvenue !</h2>
        <p>Connecte toi pour avoir accès à toutes les informations sur ton inscription, l’événement, les autres équipes, les poules et tant d’autres !</p>
        <p>Si tu n’as pas encore de compte, inscris toi vite. </p>
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-inputs">
            <%= f.input :email,
                        label: 'Votre adresse mail',
                        placeholder: 'monadressemail@gmail.com',
                        required: false,
                        autofocus: true,
                        input_html: { autocomplete: "email" } %>
            <%= f.input :password,
                        label: 'Votre mot de passe',
                        placeholder: '********',
                        required: false,
                        input_html: { autocomplete: "current-password" } %>
            <%= f.input :remember_me, label: 'Se souvenir de moi', as: :boolean if devise_mapping.rememberable? %>
          </div>
          <div class="form-actions">
            <%= f.button :submit, "Se connecter", class: "btn btn-primary" %>
          </div>
        <% end %>
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>

