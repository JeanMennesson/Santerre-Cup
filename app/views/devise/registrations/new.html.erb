<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 offset-sm-1 col-md-6 offset-md-3 col">
      <div class="form-login">
        <h2>Salut l’ami(e) ! Nous sommes ravis que tu veilles nous rejoindre pour ce bel événement !</h2>
        <p>Avant de pouvoir t’inscrire au tournoi de cette IVème édition de la Santerre Cup, il te faut d’abord te <strong>créer un compte</strong> sur notre site. Cela nous aide à fluidifier les inscriptions.</p>
        <% resource.profile || resource.build_profile %>
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= f.error_notification %>
          <div class="form-inputs">
            <%= f.input :email,
                        label: 'Votre adresse mail',
                        placeholder: 'monadressemail@gmail.com',
                        required: true,
                        autofocus: true,
                        input_html: { autocomplete: "email" }%>
            <%= f.input :password,
                        label: 'Votre mot de passe',
                        placeholder: '********',
                        required: true,
                        hint: ("#{@minimum_password_length} caractères minimum" if @minimum_password_length),
                        input_html: { autocomplete: "new-password" } %>
            <%= f.input :password_confirmation,
                        label: 'Confirmez votre mot de passe',
                        placeholder: '********',
                        required: true,
                        input_html: { autocomplete: "new-password" } %>
          </div>
          <div class="form-actions">
            <%= f.button :submit, "Créer mon compte", class: "btn btn-primary" %>
          </div>
        <% end %>
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
